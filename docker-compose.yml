services:

  backend:
    build:
      context: ./backend

    container_name: task-backend

    ports:
      - "5000:5000"

  frontend:
    build:
      context: ./frontend

    container_name: task-frontend

    ports:
      - "3000:80"

    depends_on:
      - backend
